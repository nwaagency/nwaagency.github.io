{
  "name": "nexora-agency",
  "version": "1.0.0",
  "private": true,
  "description": "Nexora Web Agency",
  "scripts": {
    "build": "mkdir -p dist && cp -R src/pages/* dist/ 2>/dev/null || true && cp -R src/assets dist/assets 2>/dev/null || true && cp -R public/* dist/ 2>/dev/null || true",
    "dev": "npm run build && npx serve dist"
  },
  "dependencies": {},
  "devDependencies": {}
}